# If mars-xlog is not on the CocoaPods trunk, add a source or git pod in the
# example app's ios/Podfile (NOT in the plugin podspec). Example:
#
#   target 'Runner' do
#     use_frameworks!
#     pod 'mars', :git => 'https://github.com/Tencent/mars.git', :tag => 'v1.2.5'
#     ...
#   end
#
# Alternatively vendor a prebuilt mars.framework — see xlog_plugin.podspec option (A).
